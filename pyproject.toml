[project]
name = "greeter"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = ["pulumi>=3.165.0,<4.0.0", "pulumi_random>=4.0,<5.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "ruff>=0.11.7",
]
